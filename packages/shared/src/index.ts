/**
 * @dayflow/shared — barrel export.
 *
 * Zod schemas, inferred types, and constants shared between apps/api and
 * apps/web live here. Populated in session S02 (see build/sessions/S02-shared.md).
 */

/** Marker so the package has a valid export before S02 fills it in. */
export const DAYFLOW_SHARED_VERSION = '0.1.0';
