/**
 * @dayflow/db — shared Prisma client entry point.
 *
 * Session S01 runs `prisma generate` and replaces this file with a singleton
 * `PrismaClient` export (see build/sessions/S01-database.md). Until then this is a
 * placeholder so the package resolves and typechecks without the generated client.
 */

/** Marker so the package has a valid export before S01 wires up the client. */
export const DAYFLOW_DB_VERSION = '0.1.0';
