# packages/db — `@dayflow/db` (Prisma)

> Schema exists now; migrations + seed are produced in **S01**. See
> `../../build/sessions/S01-database.md` and `../../docs/DATABASE.md`.

Owns the Prisma schema (`prisma/schema.prisma`), migrations, the generated client,
and the seed script. Any model change is a shared-contract change — announce it in
`../../build/STATE.md` "Blockers/notes" because it can invalidate other agents'
in-flight work.

```text
prisma/
├── schema.prisma     # data model (see docs/DATABASE.md)
├── migrations/       # generated by `prisma migrate` (S01)
└── seed.ts           # rich demo seed: ~30 employees + history (S01)
```
