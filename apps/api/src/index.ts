/**
 * Dayflow API entry point.
 *
 * Scaffold placeholder. The real Express server (app wiring, middleware, error
 * handling, config, /health) is built in session S03 (build/sessions/S03-api-core.md),
 * and feature modules in S04–S09. Running `npm run dev` now just prints this notice.
 */

console.info('[dayflow-api] scaffold — the Express server is implemented in session S03.');
