/**
 * Re-export of the shared Prisma client. Never instantiate a second `PrismaClient`
 * in the API — always import `prisma` from here (or `@dayflow/db` directly).
 */
export { prisma } from '@dayflow/db';
